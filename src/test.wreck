#!/usr/bin/env wreck
#(the shebang above this line is ignored by the tokenizer)

$printf("\e[33mfoo\e[0m '%d' \" \n" 1);

$curl([[ f silent S L ]] #(some comment between) 'https://archive.google/heart');

#(comment with an closing parentheses \) )

if (1 == 0) {
  #(nested #(comments #(work)))
  $printf("1 EQUALS 0? WHAT?\n");
} else {
  $printf("1 does not equal 0\n");
}

#('?' and 'if' can be used interchangeably
  (same with '?!' and else)) 
? (1 > 0) {
  $printf("1 is greater-than 0\n");
} ?! {
  $printf("1 ISN'T GREATER THAN 0?\n");
}

#( TODO: functions )

fn main() bool {
  $printf("foo\n");
  #(@std.printf("foo\n");)
  return true;
}

#)

let foo = "bar";
$printf("%s\n" foo);

#( BUG: anything past this point segfaults) 
foo = "baz";
$printf("%s\n" foo);

#(#(an intentionally unexpected token) ==)
